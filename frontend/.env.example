# No environment variables are required to run this project.
# The variables below are only needed if deploying behind a reverse proxy.

# REACT_APP_BACKEND_URL=https://your-backend-url.com
# WDS_SOCKET_PORT=443
